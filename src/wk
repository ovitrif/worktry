#!/bin/bash

show_help() {
  cat << EOF
██╗     ██╗                   ██╗   ██████████╗
██║     ██║                   ██║   ╚═══██╔═══╝
██║ ██╗ ██║  ██████╗  ██████╗ ██║ ██╗   ██║  ██████╗ ██╗   ██╗
██║████╗██║ ██╚═══██║ ██╔═══╝ █████╔╝   ██║  ██╔═══╝  ██╗ ██╔╝
 ███║ ███╔╝  ██████╔╝ ██║     ██╔═██╗   ██║  ██║       ████╔╝
 ╚══╝ ╚══╝   ╚═════╝  ╚═╝     ╚═╝ ╚═╝   ╚═╝  ╚═╝        ██╔╝
 ██ Vibe code in parallel using git worktrees or clones ██║
 █████████████████████████████████████████████████████████║
 ╚════════════════════════════════════════════════════════╝

USAGE:
wk <command> [options]
wk <0-9>                        Quick navigation by index

COMMANDS:
init, i                   Initialize for worktree mode
init --clone, i -c        Initialize for clone mode
list, ls, l               List worktrees or clones (with type)
go <name>                 Navigate to worktree by branch name
back, b                   Navigate back to main worktree
config, c                 Edit .worktreeinclude config
setup <dir>               Apply wk setup to a clone
new <name> [-b BASE]      Create worktree with setup
-h, --help                Show this help

NAVIGATION:
wk 0                      Jump to main repo
wk 1                      Jump to first worktree/clone
wk 2-9                    Jump to 2nd-9th worktree/clone

FILES:
.worktreeinclude          Patterns for files to copy (gitignore-style)
.worktree-setup.sh        Setup script (Claude permissions, file copying)
ALIAS: worktry

See: https://github.com/ovitrif/worktry
EOF
}

# Create .worktreeinclude file with example patterns
create_worktreeinclude() {
  cat > .worktreeinclude << 'EOF'
# Files to copy to new worktrees/clones
# Uses gitignore-style patterns
# Files must ALSO be in .gitignore to be copied

# Environment files
.env
.env.local
.env.*

# Claude local settings
**/.claude/settings.local.json

# IDE settings
.idea/
# .vscode/

# Local properties (uncomment as needed)
# local.properties
# keystore.properties
EOF
}

# Create .worktree-setup.sh script
create_setup_script() {
  cat > .worktree-setup.sh << 'SETUP_EOF'
#!/bin/bash
# Worktry setup script - runs in new worktree/clone

# Create Claude Code settings
mkdir -p .claude
cat > .claude/settings.local.json << 'JSON_EOF'
{
  "permissions": {
    "allow": [
      "Bash",
      "Read",
      "Edit",
      "Write",
      "WebFetch",
      "mcp__ide__getDiagnostics",
      "WebSearch",
      "mcp__github__pull_request_read",
      "mcp__github__search_pull_requests",
      "mcp__github__list_pull_requests"
    ],
    "deny": [],
    "ask": [
      "Bash(rm -rf:*)",
      "Bash(git commit:*)",
      "Bash(git push:*)"
    ]
  }
}
JSON_EOF

# Copy files matching BOTH .worktreeinclude AND .gitignore
copy_worktree_files() {
  local source_dir="$1"
  local include_file="$source_dir/.worktreeinclude"

  [ ! -f "$include_file" ] && return 0

  # Get all ignored files in source directory
  local ignored_files
  ignored_files=$(cd "$source_dir" && git ls-files --others --ignored --exclude-standard 2>/dev/null)

  [ -z "$ignored_files" ] && return 0

  # Read patterns from .worktreeinclude
  while IFS= read -r pattern || [ -n "$pattern" ]; do
    # Skip comments and empty lines
    [[ "$pattern" =~ ^[[:space:]]*# ]] && continue
    [[ -z "${pattern// }" ]] && continue
    pattern=$(echo "$pattern" | xargs)  # Trim whitespace

    # Match pattern against ignored files
    echo "$ignored_files" | while read -r file; do
      [ -z "$file" ] && continue

      # Pattern matching: handle glob patterns
      local matched=false

      # Direct match
      if [[ "$file" == $pattern ]]; then
        matched=true
      # Directory pattern (pattern/)
      elif [[ "$pattern" == */ ]] && [[ "$file" == ${pattern}* ]]; then
        matched=true
      # Wildcard pattern (*.ext, .env.*)
      elif [[ "$file" == $pattern ]]; then
        matched=true
      # Double-star pattern (**/file)
      elif [[ "$pattern" == \*\*/* ]]; then
        local suffix="${pattern#\*\*/}"
        if [[ "$file" == *"$suffix" ]] || [[ "$file" == "$suffix" ]]; then
          matched=true
        fi
      fi

      if [ "$matched" = true ]; then
        local src="$source_dir/$file"
        if [ -e "$src" ]; then
          mkdir -p "$(dirname "$file")"
          cp -r "$src" "$file"
          echo "  Copied: $file"
        fi
      fi
    done
  done < "$include_file"
}

copy_worktree_files "$ROOT_WORKTREE_PATH"
SETUP_EOF
  chmod +x .worktree-setup.sh
}

# Initialize for worktree mode
init_worktree_mode() {
  if [ -f ".worktreeinclude" ]; then
    echo "Error: .worktreeinclude already exists."
    echo "Remove it first or edit manually."
    exit 1
  fi

  create_worktreeinclude
  create_setup_script

  echo "Created:"
  echo "  .worktreeinclude      # Add patterns for files to copy"
  echo "  .worktree-setup.sh    # Setup script"
  echo ""
  echo "Next steps:"
  echo "  1. Edit .worktreeinclude with files to copy"
  echo "  2. wk new <branch>         # Create worktree with setup"
}

# Initialize for clone mode
init_clone_mode() {
  if [ -f ".worktreeinclude" ]; then
    echo "Error: .worktreeinclude already exists."
    echo "Remove it first or edit manually."
    exit 1
  fi

  create_worktreeinclude
  create_setup_script

  echo "Clone mode initialized!"
  echo ""
  echo "Created:"
  echo "  .worktreeinclude      # Add patterns for files to copy"
  echo "  .worktree-setup.sh    # Setup script"
  echo ""
  echo "Usage:"
  echo "  gh repo clone user/repo repo-2    # Clone"
  echo "  wk setup ../repo-2                # Apply setup"
}

create_worktree() {
  local name=""
  local base_branch="HEAD"
  
  # Parse arguments
  while [[ $# -gt 0 ]]; do
    case "$1" in
      --base|-b)
        base_branch="$2"
        shift 2
        ;;
      *)
        if [ -z "$name" ]; then
          name="$1"
        else
          echo "Error: Unknown argument '$1'" >&2
          echo "Usage: wk new <NAME> [--base|-b BASE_BRANCH]" >&2
          exit 1
        fi
        shift
        ;;
    esac
  done

  if [ -z "$name" ]; then
    echo "Error: Name argument is required."
    echo "Usage: wk new <NAME> [--base|-b BASE_BRANCH]"
    exit 1
  fi

  TARGET_PATH=".claude/worktrees/${name}"

  echo "Creating worktree '$name' at '$TARGET_PATH' from '$base_branch'..."
  git worktree add "$TARGET_PATH" -b "$name" "$base_branch"

  # Run setup if .worktree-setup.sh exists
  local source_dir="$PWD"
  if [ -f ".worktree-setup.sh" ]; then
    echo "Running setup..."
    (cd "$TARGET_PATH" && ROOT_WORKTREE_PATH="$source_dir" bash "$source_dir/.worktree-setup.sh")
  fi
}

# Navigate to a worktree by branch name
go_to_worktree() {
  local name="$1"
  if [ -z "$name" ]; then
    echo "Error: Branch name required." >&2
    echo "Usage: wk go <branch-name>" >&2
    exit 1
  fi

  # Find worktree path by branch name
  local path=$(git worktree list --porcelain 2>/dev/null | awk -v branch="$name" '
    /^worktree / { wt = substr($0, 10) }
    /^branch / { if ($0 ~ branch "$") print wt }
  ')

  if [ -z "$path" ]; then
    echo "Error: No worktree found for branch '$name'" >&2
    exit 1
  fi

  # Output path for shell function to cd
  echo "$path"
}

# Navigate back to main worktree
go_back() {
  # Main worktree is the one without "branch" being a linked worktree
  # It's typically the first one listed, or we find the one that's not bare
  local main_path=$(git worktree list --porcelain 2>/dev/null | awk '
    /^worktree / { wt = substr($0, 10) }
    /^bare$/ { next }
    /^HEAD / { if (wt) { print wt; exit } }
  ')

  if [ -z "$main_path" ]; then
    # Fallback: first worktree
    main_path=$(git worktree list 2>/dev/null | head -1 | awk '{print $1}')
  fi

  if [ -z "$main_path" ]; then
    echo "Error: Could not find main worktree" >&2
    exit 1
  fi

  echo "$main_path"
}

# Collect all entries (worktrees + sibling clones, deduplicated)
# Output: path|type lines
collect_entries() {
  local main_path
  main_path=$(git worktree list 2>/dev/null | head -1 | awk '{print $1}')
  [ -z "$main_path" ] && main_path=$(pwd)

  local parent_dir base_name
  parent_dir=$(dirname "$main_path")
  base_name=$(basename "$main_path" | sed -E 's/[-.]?[0-9]+$//' | sed -E 's/\.[^.]+$//')

  # Find all sibling repos sharing the base name
  local siblings=()
  while IFS= read -r dir; do
    if [[ -d "$dir/.git" || -f "$dir/.git" ]]; then
      siblings+=("$dir")
    fi
  done < <(find "$parent_dir" -maxdepth 1 -type d -name "${base_name}*" 2>/dev/null | sort)

  # Fallback: no siblings found, list current repo's worktrees
  if [[ ${#siblings[@]} -eq 0 ]]; then
    git worktree list 2>/dev/null | while IFS= read -r line; do
      echo "$(echo "$line" | awk '{print $1}')|worktree"
    done
    return
  fi

  # For each sibling, list its worktrees or mark as clone
  for dir in "${siblings[@]}"; do
    local wt_count
    wt_count=$(git -C "$dir" worktree list 2>/dev/null | wc -l | tr -d ' ')

    if [[ "$wt_count" -gt 1 ]]; then
      git -C "$dir" worktree list 2>/dev/null | while IFS= read -r wt_line; do
        echo "$(echo "$wt_line" | awk '{print $1}')|worktree"
      done
    else
      if [[ ${#siblings[@]} -gt 1 ]]; then
        echo "$dir|clone"
      else
        echo "$dir|worktree"
      fi
    fi
  done | awk -F'|' '!seen[$1]++'
}

# Navigate to worktree/clone by index (0 = first, 1-9 = others)
go_to_index() {
  local index="$1"

  local entries=()
  while IFS= read -r line; do
    [[ -n "$line" ]] && entries+=("${line%|*}")
  done < <(collect_entries)

  if [[ $index -ge ${#entries[@]} ]]; then
    echo "Error: No entry at index $index" >&2
    echo "" >&2
    echo "Available:" >&2
    list_indexed >&2
    exit 1
  fi

  echo "${entries[$index]}"
}

# List all worktrees and clones with aligned columns
list_indexed() {
  local current_dir
  current_dir=$(pwd -P)

  local paths=() types=()
  while IFS= read -r line; do
    [[ -z "$line" ]] && continue
    paths+=("${line%|*}")
    types+=("${line##*|}")
  done < <(collect_entries)

  # Find max path length for column alignment
  local max_len=0
  for p in "${paths[@]}"; do
    [[ ${#p} -gt $max_len ]] && max_len=${#p}
  done

  # Find max type length for alignment
  local max_type=0
  for t in "${types[@]}"; do
    local label="[$t]"
    [[ ${#label} -gt $max_type ]] && max_type=${#label}
  done

  for i in "${!paths[@]}"; do
    local path="${paths[$i]}"
    local type="${types[$i]}"
    local label="[$type]"
    local resolved
    resolved=$(cd "$path" 2>/dev/null && pwd -P)
    local marker=""
    [[ "$resolved" == "$current_dir" ]] && marker=" *"
    printf "%d  %-${max_len}s  %-${max_type}s%s\n" "$i" "$path" "$label" "$marker"
  done
}

# Apply worktry setup to an existing clone directory
setup_clone() {
  local target_dir="$1"
  
  if [ -z "$target_dir" ]; then
    echo "Error: Directory argument required." >&2
    echo "Usage: wk setup <directory>" >&2
    exit 1
  fi
  
  # Resolve to absolute path
  if [[ ! "$target_dir" = /* ]]; then
    target_dir="$(pwd)/$target_dir"
  fi
  
  if [ ! -d "$target_dir" ]; then
    echo "Error: Directory '$target_dir' does not exist." >&2
    exit 1
  fi
  
  if [ ! -d "$target_dir/.git" ] && [ ! -f "$target_dir/.git" ]; then
    echo "Error: '$target_dir' is not a git repository." >&2
    exit 1
  fi
  
  local source_dir=$(pwd)
  
  if [ ! -f "$source_dir/.worktree-setup.sh" ]; then
    echo "Error: No .worktree-setup.sh in current directory." >&2
    echo "Run 'wk init' first." >&2
    exit 1
  fi
  
  echo "Applying wk setup to $target_dir..."
  (cd "$target_dir" && ROOT_WORKTREE_PATH="$source_dir" bash "$source_dir/.worktree-setup.sh")
  echo "✓ Setup complete"
}

# Edit .worktreeinclude config file
edit_config() {
  local configfile=".worktreeinclude"

  if [ ! -f "$configfile" ]; then
    echo "Error: $configfile not found." >&2
    echo "Run 'wk init' first." >&2
    exit 1
  fi

  local editor="${EDITOR:-${VISUAL:-}}"

  if [ -n "$editor" ]; then
    $editor "$configfile"
  else
    echo "No default text editor configured."
    echo ""
    echo "To set one, add to your shell config (~/.zshrc or ~/.bashrc):"
    echo "  export EDITOR=nano      # or vim, subl, code, etc."
    echo ""
    read -p "Open with VSCode instead? [y/N] " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
      code "$configfile"
    fi
  fi
}

case "$1" in
  init|i)
    shift
    case "$1" in
      --clone|-c)
        init_clone_mode
        ;;
      "")
        init_worktree_mode
        ;;
      *)
        echo "Error: Unknown option '$1'" >&2
        echo "Usage: wk init [--clone]" >&2
        exit 1
        ;;
    esac
    ;;
  go)
    go_to_worktree "$2"
    ;;
  back|b)
    go_back
    ;;
  list|ls|l)
    list_indexed
    ;;
  config|c)
    edit_config
    ;;
  setup|s)
    setup_clone "$2"
    ;;
  new|n)
    shift
    create_worktree "$@"
    ;;
  -h|--help|"")
    show_help
    ;;
  [0-9])
    go_to_index "$1"
    ;;
  *)
    echo "Error: Unknown command '$1'"
    echo ""
    show_help
    exit 1
    ;;
esac
